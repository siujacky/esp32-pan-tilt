# PlatformIO build output + downloaded libraries (regenerated by `pio run`)
.pio/

# IDE state
.vscode/
*.code-workspace

# vendor reference library (nulllab mini-joystick) - not compiled, license not ours to publish;
# fetch from https://github.com/nulllaborg/mini-joystick-module if you want the original
joystick_handle/

# local scratch
*.log
